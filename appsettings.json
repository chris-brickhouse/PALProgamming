// this is the JSON object that the PALModels.cs class will import the database connection string from
// ACTION: after creating, make sure to right click the file, click properties and change "Copy to Output Directory" to "copy always". this will ensure the file will always be compiled into the same folder as the app.

{
  "ConnectionStrings": {
    "Students": "Server=localhost;Database=students;Trusted_Connection=true;"
  }
}
